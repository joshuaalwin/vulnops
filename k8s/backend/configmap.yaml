apiVersion: v1
kind: ConfigMap
metadata:
  name: vulnops-backend-config
  namespace: vulnops
data:
  # Set this to the NLB hostname after provisioning, e.g.:
  # https://a1b2c3d4e5f6.elb.us-east-1.amazonaws.com
  # Multiple origins: comma-separated
  ALLOWED_ORIGINS: ""
