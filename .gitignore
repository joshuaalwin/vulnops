node_modules/
dist/
.env
*.log
.DS_Store

# Claude
CLAUDE.md
.claude/

# Local dev log (synced to Notion separately)
DEVLOG.md

# Personal learning notes (local only)
learning/

# Terraform
terraform/.terraform/
terraform/tfplan
terraform/terraform.tfvars
terraform/*.zip
terraform.tfstate
terraform.tfstate.backup
terraform/*.tfstate
terraform/*.tfstate.backup

# Kubernetes secrets — never commit real credentials
k8s/secrets.yaml
docs/superpowers/
