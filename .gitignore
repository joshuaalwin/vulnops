node_modules/
dist/
.env
*.log
.DS_Store

# Claude
CLAUDE.md
.claude/

# Local dev log (synced to Notion separately)
DEVLOG.md

# Personal learning notes (local only)
learning/

# Terraform
terraform/.terraform/
terraform/tfplan
terraform/terraform.tfvars
terraform/*.zip
terraform.tfstate
terraform.tfstate.backup
terraform/*.tfstate
terraform/*.tfstate.backup

# Kubernetes secrets — never commit real credentials
k8s/secrets.yaml
docs/superpowers/

# Architecture diagram tooling (kept local)
scripts/.venv/
scripts/render-architecture.py
scripts/make-icons.py
scripts/assets/

# PNGs live on the GitHub release (tag: static-assets), not in the repo
*.png

# Banner/logo SVGs also live on the GitHub release
/VulnOps-Logo.svg
/Vulnops-banner.svg
